const countCharactersInString = require('./count-characters-in-string')

describe('', () => { 
    it('should', () => { 
        expect(countCharactersInString("Hello, World!")).toBe(13);
        // expect(countCharactersInString("This is a test string.")).toBe(4);
        // expect(countCharactersInString("NoSpacesHere")).toBe(0);
        // expect(countCharactersInString("  Two spaces at the start")).toBe(2);

    })
})